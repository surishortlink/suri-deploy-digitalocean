spec:
  name: suri
  static_sites:
    - name: suri
      build_command: npm run build
      output_dir: build
      github:
        repo: {{owner}}/{{repo}}}
        branch: main
        deploy_on_push: true
